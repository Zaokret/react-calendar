# Description

This is a responsive GitHub-style activity heatmap built with React, TypeScript, and CSS Grid.

The component supports:
- arbitrary date ranges
- configurable week start offsets
- horizontal and vertical orientations 
- responsive scaling via rem
- month-aware hover highlighting

# Run steps

`npm install`

`npm dev`


